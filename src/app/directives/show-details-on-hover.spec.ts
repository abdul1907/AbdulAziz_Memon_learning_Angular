import { ShowDetailsOnHover } from './show-details-on-hover';

describe('ShowDetailsOnHover', () => {
  it('should create an instance', () => {
    const directive = new ShowDetailsOnHover();
    expect(directive).toBeTruthy();
  });
});
